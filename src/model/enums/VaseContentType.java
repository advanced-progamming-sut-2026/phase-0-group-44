package model.enums;

/** Canonical Vasebreaker vase outcomes, including the two special vase kinds. */
public enum VaseContentType {
    EMPTY,
    ZOMBIE,
    SEED_PACKET,
    PLANT_VASE,
    GARGANTUAR_VASE
}
